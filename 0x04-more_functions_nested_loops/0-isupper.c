#include "main.h"
/**
 * _isupper - checks for upper case letters
 * @c: argument
 * Return: upper
 */
int _isupper(int c)
{
	int upper;

	if (c >= 'A' && c <= 'Z')
	{
		upper = 1;
	}
	else
	{
		uppeer = 0;
	}

	return (upper);
}
